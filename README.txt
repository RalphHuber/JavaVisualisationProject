To run our visaulisation submission
	in the directory final/main- compile and run PathFindingGUI.java

To run our test code
	in final/tests - move the test classes into the main directory and run as usual
